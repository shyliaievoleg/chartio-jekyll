---
label: homepage
hide_body: true
fields:
- name: home_sections
  type: blocks
  label: home sections
  template_types:
  - home-welcome-section
  - what-we-do
  - home-services
  config:
    min: 
    max: 
